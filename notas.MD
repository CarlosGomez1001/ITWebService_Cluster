
# Para construir proyecto con proxy en prod
docker compose --profile prod --profile proxy up -d --build   

# Para levantar proyecto con proxy en prod
docker compose --profile prod --profile proxy up -d 